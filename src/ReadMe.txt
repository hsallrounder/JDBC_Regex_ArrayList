Create Database with name Atest
create a table with name student having 3 fields:
Rollno int
Name varchar
marks int

We will perform 3 operations on database
insert
delete
select
you have to create different methods to perform this.

Use collection class ArrayList to store 10 objects of Student class,
you have to insert only those records inside database who name ends with letter j/J.

Use try with resource to auto-close the connection.